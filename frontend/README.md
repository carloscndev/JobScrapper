# Frontend

The frontend will host the React and TypeScript dashboard. It will consume the
backend's documented API and should not contain scraping, persistence, or
secret-handling logic.

Ownership: frontend coder tasks. Component and end-to-end tests belong in
`tests/` or the frontend's configured test directory when introduced.
