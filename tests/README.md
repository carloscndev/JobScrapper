# Tests

Tests are organized by subsystem as implementation begins. Harness tests live
in `tests/harness/`; backend, frontend, integration, and end-to-end suites may
use dedicated subdirectories without mixing production code into this tree.
